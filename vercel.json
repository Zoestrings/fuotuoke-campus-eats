{
  "buildCommand": "npm install --prefix frontend && CI=false npm run build --prefix frontend",
  "outputDirectory": "frontend/build",

  "cleanUrls": true,
  "rewrites": [
    { "source": "/((?!api|static|.*\\..*).*)", "destination": "/index.html" }
  ]
}
